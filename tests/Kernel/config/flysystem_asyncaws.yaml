flysystem:
    storages:
        fs_asyncaws:
            adapter: 'asyncaws'
            options:
                client: 'asyncaws_client_service'
                bucket: '%env(AWS_BUCKET)%'
                prefix: 'optional/path/prefix'
